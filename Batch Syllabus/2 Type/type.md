1. Type inference : TS infert type from the assinged value & its automatically assign to variable

2. Type annotation : user enforcing the type for holding value inside the variable
   Syntax:
   variableName : type

For .ts can compile automatically:
tsc fileName.ts --watch
tsc fileName.ts -w

# Primitive Datatype:

string
number
boolean
null
undefined
unknown
never
void

# Special datatype:

any

# Non-Primitive datatype:

class
object
function
interface
array
tuple
enum

