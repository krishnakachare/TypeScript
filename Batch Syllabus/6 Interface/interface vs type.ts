/*
# type --> 
- define type, structure (literal type)
- literal type --> object, fuction, class ?

type->object litral

let obj: {
  name: string;
  age: number;
} = {
  name: "Hi",
  age: 12,
};

type->function litral
(para: number, para2: number) => number;

type -> type literal no for class, only type annotate in class defination
- used for specific primitive datatype
- Can able to do Intersection(&), Union(|)

==========================================

# interface --> 
- defining structure for object, function, class
- used for specific non-primitive datatype
- can able to do extends, merging 

*/
