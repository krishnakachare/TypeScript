/*  
OOPS: Polymorphism => overloading + overriding

overloading: same object/class, same method name, different signature

        Abc.method1(par1, para2)
        Abc.method1(par3, para4)
        Abc.method1(par5, para6)

        .should("be.visible")
        .should("have.text", "")
        .should("have.class", "className", "classVale")

overriding: different object/class, same method name, same signature

        Abc1.method1(par1, para2)
        Abc2.method1(par1, para2)
        Abc3.method1(par1, para2)

        grandFather.printFullName(); // RAM KK
        father.printFullName(); // sham KK
*/
